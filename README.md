# これはなに？

このフォルダは、LLM活用を試す小さなサンプルアプリをまとめた置き場です。現在は SQL マート改修の学習用アプリを `mart-llm-editor/` 配下に入れています。

# 中身

- `mart-llm-editor/`: Google Gemini を使って既存 SQL マートを改修するデモアプリ。

今後、別の小規模デモを追加する場合も、この直下にディレクトリを増やしていく想定です。
